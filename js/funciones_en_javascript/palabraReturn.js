
function miFuncion(a, b) {
    // La palabra clave "return" devuelve la suma de los parámetros a y b y finaliza la ejecución de la función
    // Si no se usa return, la función devuelve undefined por defecto, asi sea almacenada en una variable.
    return a + b;
}

let resultado = miFuncion(4,5);
console.log(resultado);